export function get_featured_thumbnail(post_id) {
  //   const post = posts.find((p) => p.id === post_id);
  //   if (!post) return null;
  //   const thumbnail = post?.attachment_metadata?.sizes?.find((item) => item.slug === 'thumbnail');
  //   return thumbnail?.file || post.filename || null;
}
